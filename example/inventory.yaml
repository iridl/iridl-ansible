all:
  children:
    dlservers:
      vars:
        ansible_become: true
      hosts:
        datalibrary.example.com:

        # If your server doesn't have a DNS name, only an IP address:
        # dlserver1:
        #   ansible_host: 192.0.2.53

        # If ssh is on a non-standard port:
        # datalibrary.example.com:
        #   ansible_port: 1234
