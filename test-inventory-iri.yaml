# Inventory variable overrides for use when running the tests at IRI.

all:
  vars:
    # Public NTP# servers are blocked inside the IRI network, so
    # without this override the chronyc test fails when run at IRI.
    ntp_servers:
      - irione.iri.columbia.edu
      - iritwo.iri.columbia.edu
      
